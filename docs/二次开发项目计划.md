# sfwork 二次开发项目计划

> 基于 sfwork 工作区（Kuscia + SecretFlow + SecretPad + SecretPad Frontend）的二次开发项目计划。  
> 目标：在现有联邦学习平台基础上，完成**本地差分隐私**、**数据安全分类分级**、**平台熟练使用**三大能力建设，并可持续交付、可复用、可演示。

---

## 1. 项目背景与目标

### 1.1 项目背景

sfwork 是 SecretFlow 隐私计算生态的本地开发工作区，核心包含：

| 项目 | 技术栈 | 职责 |
|---|---|---|
| Kuscia | Go / Kubernetes / gRPC | 任务调度、节点通信、镜像管理 |
| SecretFlow | Python (JAX/NumPy/SPU/HEU) | 隐私计算算法与组件实现 |
| SecretPad | Java / Spring Boot | Web 管理后台、REST API、数据管理 |
| SecretPad Frontend | TypeScript / React / Umi | 前端界面、流水线画布、组件面板 |

当前工作区已完成：

- 自定义隐私计算镜像 `secretflow/sf-privacy-dev:1.15.0.dev-privacy` 的构建与注册；
- `privacy/l_diversity` 组件的成功集成与端到端验证；
- 前后端 + Kuscia 容器的本地开发联调通路已打通。

### 1.2 项目目标

1. **本地差分隐私（Local DP）**
   - 实现可在单节点本地执行的差分隐私组件，支持 Laplace / Gaussian 噪声注入；
   - 集成到 SecretPad 组件面板、流水线模板、自定义镜像；
   - 提供可配置的隐私预算（ε/δ）与敏感度计算策略。

2. **数据安全分类分级**
   - 建立数据分级标准（如 L1 公开 / L2 内部 / L3 机密 / L4 绝密）；
   - 实现自动识别规则引擎（关键字、正则、统计特征、可选 ML 模型）；
   - 在 SecretPad 中提供分类分级结果展示、策略管理、审批流转能力。

3. **现有联邦学习平台熟练使用**
   - 沉淀标准操作流程（SOP）与最佳实践文档；
   - 提供可复用的联邦学习流水线模板（LR、XGB、神经网络、特征工程）；
   - 建立前端引导式教程与技能测评机制，降低新用户上手门槛。

---

## 2. 总体技术路线

```text
需求分析 → 算法/产品设计 → 分层开发 → 单元/集成/E2E 测试 → 镜像构建 → Kuscia 部署 → 培训交付 → 持续迭代
```

### 2.1 关键技术决策

| 决策项 | 方案 | 理由 |
|---|---|---|
| 算法实现层 | SecretFlow Python 组件 | 与现有 `privacy/l_diversity` 对齐，复用组件注册与镜像机制 |
| 元数据与配置 | SecretPad `config/components/*.json` + `i18n/*.json` | 已有成熟的组件加载与国际化机制 |
| 数据分类分级 | SecretFlow 组件 + SecretPad 后端治理模块 | 敏感数据扫描在本地完成，结果同步到中心展示 |
| 平台教程 | 前端模板 + Markdown 文档 + 示例项目 | 不侵入核心代码，可独立迭代 |
| 部署方式 | Docker 容器化 + `scripts/dev-start.sh` | 与当前工作区保持一致，便于二次开发验证 |

---

## 3. 主要功能模块详细设计

### 3.1 本地差分隐私（Local DP）

#### 3.1.1 功能定义

在数据不出域的前提下，对本地数据表添加符合差分隐私要求的噪声，生成可用于下游联邦学习或联合统计的脱敏数据。

#### 3.1.2 算法设计

1. **输入**：本地数据表（`VerticalTable` / `IndividualTable`）、列类型、预算参数。
2. **参数**：
   - `epsilon`：隐私预算 ε，默认 `1.0`；
   - `delta`：失败概率 δ，Gaussian 机制时使用；
   - `mechanism`：`laplace` / `gaussian` / `none`；
   - `clip_lower` / `clip_upper`：数值列裁剪边界；
   - `sensitivity_mode`：`global`（用户提供）/ `auto`（基于分位数估计）。
3. **处理流程**：
   - 按列识别数据类型（数值 / 类别 / 文本）；
   - 对数值列做 L1 / L2 裁剪；
   - 根据机制注入噪声：`Lap(Δf/ε)` 或 `N(0, 2Δf²·ln(1/δ)/ε²)`；
   - 输出带噪声的数据表与噪声统计报告（均值、方差、实际 ε/δ）。
4. **预算管理**（可选增强）：
   - 单表级别预算池，避免重复调用导致预算耗尽；
   - 与 `privacy/differential_privacy` 全局 DP 组件形成互补。

#### 3.1.3 代码实现范围

| 仓库 | 目录/文件 | 工作项 |
|---|---|---|
| secretflow | `secretflow/component/privacy/local_dp.py` | 实现 Local DP 组件核心逻辑 |
| secretflow | `secretflow/component/privacy/__init__.py` | 注册新组件 |
| secretflow | `tests/component/privacy/test_local_dp.py` | 单元测试 |
| secretpad | `config/components/secretflow.json` | 添加组件元数据 |
| secretpad | `config/i18n/secretflow.json` | 添加中英文国际化 |
| secretpad-frontend | `apps/platform/src/modules/template/...` | 可选：Local DP 快速模板 |
| secretflow | `docker/privacy-dev/Dockerfile` | 确保新组件打入自定义镜像 |

---

### 3.2 数据安全分类分级

#### 3.2.1 功能定义

对平台接入的数据集进行自动化、可配置的安全分类与分级，帮助数据所有者识别敏感字段、制定脱敏/访问策略，并支持分级审批。

#### 3.2.2 分级标准示例

| 等级 | 名称 | 示例字段 | 处理策略 |
|---|---|---|---|
| L1 | 公开 | 用户 ID 哈希、统计指标 | 可自由共享 |
| L2 | 内部 | 年龄区间、城市 | 脱敏后可共享 |
| L3 | 机密 | 手机号、身份证号、收入 | 需授权 + 加密/本地 DP |
| L4 | 绝密 | 生物特征、疾病诊断 | 禁止出域，仅本地使用 |

#### 3.2.3 自动识别规则引擎

1. **规则层**：
   - 关键字规则：姓名、身份证、银行卡、手机号、地址、病史等；
   - 正则规则：中国大陆身份证、手机号、邮箱、银行卡号；
   - 统计规则：唯一值比例 > 阈值 → 高敏感；熵值异常 → 潜在标识符。
2. **模型层（可选增强）**：
   - 使用预训练的小模型或正则+词表组合进行列语义识别；
   - 在 SecretFlow 中通过本地执行，不上传原始数据。
3. **输出**：
   - 每张表的分类分级报告（JSON）；
   - 每列的等级、置信度、命中规则、建议处理策略。

#### 3.2.4 代码实现范围

| 仓库 | 目录/文件 | 工作项 |
|---|---|---|
| secretflow | `secretflow/component/security/data_classification.py` | 本地分类分级扫描组件 |
| secretflow | `secretflow/security/classification/rules*.yaml` | 规则配置 |
| secretpad | 后端新增 `ClassificationService`、`ClassificationController` | 策略管理、结果查询 |
| secretpad | 新增 JPA Entity：`DataClassificationDO`、`ClassificationRuleDO` | 持久化分级结果与规则 |
| secretpad | `config/schema/center/VX__classification.sql` | Flyway 迁移脚本 |
| secretpad-frontend | 新增页面：数据分级结果、规则管理、审批详情 | 前端 UI |
| secretpad | `config/components/secretflow.json` + `i18n/secretflow.json` | 注册扫描组件 |

---

### 3.3 联邦学习平台熟练使用

#### 3.3.1 目标

将团队对 SecretFlow / SecretPad / Kuscia 的使用经验沉淀为可复用资产，缩短新成员与业务方的上手周期。

#### 3.3.2 建设内容

1. **标准操作流程（SOP）**
   - 节点接入与授权；
   - 数据注册与样本对齐；
   - 组件选择与参数配置；
   - 任务运行、日志排查、结果导出。
2. **可复用流水线模板**
   - 横向联邦 LR；
   - 横向联邦 XGBoost；
   - 纵向联邦特征工程 + 训练；
   - 联邦评估与模型服务。
3. **前端引导式教程**
   - 登录后新手引导；
   - 创建第一个联邦学习项目 Wizard；
   - 关键页面 Tooltip / 视频弹窗。
4. **技能测评与认证**
   - 在线问卷 + 实操任务评分；
   - 平台使用熟练度报告。

#### 3.3.3 代码实现范围

| 仓库 | 目录/文件 | 工作项 |
|---|---|---|
| secretpad-frontend | `apps/platform/src/modules/template/pipeline-template-*.ts` | 新增联邦学习模板 |
| secretpad-frontend | `apps/platform/src/modules/guide/...` | 新手引导组件 |
| secretpad | 后端新增 `TemplateService` 扩展 | 模板分类、权限 |
| sfwork/docs | `docs/联邦学习平台使用手册.md` | SOP 文档 |
| sfwork/docs | `docs/最佳实践与常见问题.md` | 运维与调优指南 |

---

## 4. 详细工作计划

### Phase 0：需求调研与环境准备（第 1–2 周）

| 序号 | 任务 | 负责人 | 交付物 | 验收标准 |
|---|---|---|---|---|
| 0.1 | 明确三大功能的业务场景、合规要求、性能指标 | 产品经理 + 算法 | 需求规格说明书 | 评审通过 |
| 0.2 | 复现并掌握当前 sfwork 一键启动流程 | 全栈/后端 | 可运行的本地环境 | `scripts/dev-start.sh` 成功 |
| 0.3 | 学习现有 `privacy/l_diversity` 与 `privacy/differential_privacy` 源码 | 算法 | 源码阅读笔记 | 输出组件注册流程图 |
| 0.4 | 梳理 SecretPad 数据管理、节点管理、流水线后端 API | 后端/前端 | 接口清单 | 输出 API 映射表 |
| 0.5 | 制定数据分级标准与规则初稿 | 安全/产品 | 分类分级标准 V1.0 | 评审通过 |

### Phase 1：算法与产品设计（第 3–4 周）

| 序号 | 任务 | 负责人 | 交付物 | 验收标准 |
|---|---|---|---|---|
| 1.1 | Local DP 算法设计：敏感度、噪声机制、预算管理 | 算法 | 算法设计文档 | 数学推导与伪代码 |
| 1.2 | 数据分类分级算法设计：规则引擎、输出格式 | 算法 + 后端 | 分类分级设计文档 | 规则覆盖度 ≥ 80% |
| 1.3 | SecretPad 后端数据模型与 API 设计（分类分级） | 后端 | API/DB 设计文档 | Swagger/ER 图 |
| 1.4 | 前端页面与交互设计（分类分级 + 引导教程） | 前端 + 产品 | Figma/原型 | 评审通过 |
| 1.5 | 平台熟练度 SOP 与模板清单设计 | 产品 + 算法 | 模板清单 | 覆盖 4+ 典型场景 |

### Phase 2：核心代码开发（第 5–10 周）

#### 2.1 SecretFlow 算法层

| 序号 | 任务 | 实现要点 | 参考目录 |
|---|---|---|---|
| 2.1.1 | Local DP 组件开发 | 继承组件框架，实现 `execute`/`evaluate` | `secretflow/component/privacy/` |
| 2.1.2 | Local DP 单元测试 | 模拟数据验证 ε-差分隐私、均值保留 | `tests/component/privacy/` |
| 2.1.3 | 分类分级组件开发 | 规则加载、列扫描、报告输出 | `secretflow/component/security/` |
| 2.1.4 | 分类分级单元测试 | 覆盖身份证、手机号、收入等样例 | `tests/component/security/` |
| 2.1.5 | 构建自定义镜像 | 更新 `docker/privacy-dev/Dockerfile`，加入自检 | `secretflow/docker/privacy-dev/` |

#### 2.2 SecretPad 后端

| 序号 | 任务 | 实现要点 | 参考目录 |
|---|---|---|---|
| 2.2.1 | 分类分级数据模型与 Flyway 脚本 | JPA Entity + `schema/center/VX__*.sql` | `secretpad-persistence` |
| 2.2.2 | 分类分级 Service/Controller | REST API：`/api/v1alpha1/dataclassification/*` | `secretpad-service`、`secretpad-web` |
| 2.2.3 | 注册 SecretFlow 新组件元数据 | 更新 `config/components/secretflow.json` + `i18n` | `secretpad/config/` |
| 2.2.4 | 模板管理接口扩展 | 支持模板分类、示例项目一键导入 | `secretpad-service` |
| 2.2.5 | 后端单元测试 | JUnit + Mockito | `secretpad-web/src/test/...` |

#### 2.3 SecretPad 前端

| 序号 | 任务 | 实现要点 | 参考目录 |
|---|---|---|---|
| 2.3.1 | 分类分级结果展示页面 | 表格、等级标签、置信度、规则命中 | `apps/platform/src/pages/...` |
| 2.3.2 | 规则管理页面 | 增删改查规则、优先级、启用开关 | 同上 |
| 2.3.3 | 新增组件面板国际化 | 确认 `privacy/local_dp`、`security/data_classification` 显示正常 | `config/i18n/secretflow.json` |
| 2.3.4 | 流水线模板扩展 | 新增 Local DP 预处理模板、联邦学习模板 | `apps/platform/src/modules/template/` |
| 2.3.5 | 新手引导组件 | Wizard、Tooltip、帮助视频入口 | `apps/platform/src/modules/guide/` |
| 2.3.6 | 前端单元测试 | Jest + React Testing Library | 同模块 `__tests__` |

### Phase 3：测试验证（第 11–13 周）

| 序号 | 测试类型 | 内容 | 通过标准 |
|---|---|---|---|
| 3.1 | 单元测试 | SecretFlow pytest、后端 JUnit、前端 Jest | 核心模块覆盖率 ≥ 60% |
| 3.2 | 本地集成测试 | `dev-start.sh` 启动完整环境，运行 Local DP 与分类分级流水线 | 2/2 任务成功 |
| 3.3 | 端到端隐私验证 | 对比加噪前后数据分布、验证 ε 预算消耗 | 报告符合预期 |
| 3.4 | 分类分级准确率测试 | 使用标准数据集（含 PII）评估规则命中率 | 命中率 ≥ 85% |
| 3.5 | 性能测试 | 100 万行数据 Local DP / 分类分级耗时 | 单节点 ≤ 5 分钟（可调整） |
| 3.6 | 安全扫描 | 代码中无硬编码密钥、无敏感信息泄露 | gitleaks/手动检查通过 |

### Phase 4：部署交付（第 14–15 周）

| 序号 | 任务 | 操作要点 | 参考命令/脚本 |
|---|---|---|---|
| 4.1 | 构建并推送自定义镜像 | 包含新组件的 SecretFlow 镜像 | `docker build -t <tag> secretflow/docker/privacy-dev/` |
| 4.2 | 部署 Kuscia 集群 | 使用本地自定义镜像 | `cd secretpad && bash scripts/install-kuscia-only.sh master -P notls` |
| 4.3 | 部署 SecretPad 后端 | 编译 fat jar 并启动 | `mvn clean install -DskipTests` + `java -jar target/secretpad.jar` |
| 4.4 | 部署 SecretPad 前端 | 生产构建并配合 Nginx | `pnpm --filter secretpad build` |
| 4.5 | 用户培训 | 基于 SOP 与模板进行现场/线上培训 | `docs/联邦学习平台使用手册.md` |
| 4.6 | 交付文档 | 部署手册、运维手册、算法说明 | `docs/` 目录 |

### Phase 5：运营与持续迭代（第 16 周起）

| 序号 | 任务 | 内容 |
|---|---|---|
| 5.1 | 监控与告警 | 接入 Kuscia/SecretPad 日志，监控任务失败率 |
| 5.2 | 用户反馈收集 | 建立问题工单与功能建议通道 |
| 5.3 | 规则库迭代 | 每月更新分类分级规则，覆盖新类型敏感数据 |
| 5.4 | 性能优化 | 针对大数据量 Local DP 做采样、并行化 |
| 5.5 | 版本升级 | 跟随 SecretFlow/SecretPad 主版本更新自定义镜像 |

---

## 5. 角色分工建议

| 角色 | 人数 | 主要职责 |
|---|---|---|
| 项目经理/产品经理 | 1 | 需求管理、排期、验收、培训组织 |
| 隐私计算算法工程师 | 1–2 | Local DP、分类分级算法设计与 SecretFlow 组件开发 |
| Java 后端工程师 | 1–2 | SecretPad 后端 API、数据模型、Kuscia 集成 |
| 前端工程师 | 1 | SecretPad Frontend 页面、模板、引导组件 |
| DevOps/测试工程师 | 1 | 环境搭建、镜像构建、测试、部署 |
| 安全顾问 | 0.5 | 数据分级标准、隐私预算策略、合规审查 |

---

## 6. 里程碑与排期

| 里程碑 | 时间 | 关键交付 |
|---|---|---|
| M1：环境就绪 | 第 2 周 | 全队可本地一键启动 sfwork |
| M2：设计冻结 | 第 4 周 | 算法/产品/接口设计评审通过 |
| M3：Local DP 可用 | 第 7 周 | 组件注册成功、单节点流水线跑通 |
| M4：分类分级可用 | 第 9 周 | 自动扫描 + 结果展示功能可用 |
| M5：前端教程与模板 | 第 11 周 | 4+ 模板 + 新手引导上线 |
| M6：集成测试通过 | 第 13 周 | E2E 测试、性能/安全测试通过 |
| M7：生产部署与培训 | 第 15 周 | 镜像、文档、培训交付 |

**总体周期**：约 15 周（可根据团队规模与优先级调整）。

---

## 7. 风险与应对

| 风险 | 影响 | 应对措施 |
|---|---|---|
| SecretFlow 组件注册机制复杂 | 高 | 第 0 周深入研读 `l_diversity` 实现；复用已有 `Dockerfile` 与 `dev-start.sh` |
| 隐私预算参数选择不当导致结果失真 | 高 | 设计阶段明确业务可接受的误差范围；提供参数推荐与预览功能 |
| 分类分级规则误报/漏报 | 中 | 引入置信度与人工复核；建立规则灰度与 A/B 评估机制 |
| 大数据量下性能不达标 | 中 | 采用列采样、并行化、增量扫描；必要时使用 C++ 扩展或向量化 |
| Kuscia 端口/证书冲突 | 中 | 严格遵循 `application-dev.yaml` 与环境变量配置；文档化端口映射 |
| 前端组件元数据与后端不一致 | 中 | 统一在 `config/components/*.json` 和 `i18n/*.json` 维护；提交前做 diff 检查 |

---

## 8. 交付物清单

### 8.1 代码交付物

- SecretFlow 组件：`privacy/local_differential_privacy`、`security/data_classification`；
- SecretPad 后端：分类分级 API、数据模型、Flyway 脚本；
- SecretPad 前端：分类分级页面、规则管理、模板、新手引导；
- 自定义镜像：`secretflow/sf-privacy-dev:<version>`。

### 8.2 文档交付物

- `docs/二次开发项目计划.md`（本文档）
- `docs/本地差分隐私算法设计.md`
- `docs/数据安全分类分级标准.md`
- `docs/联邦学习平台使用手册.md`
- `docs/部署与运维手册.md`
- `docs/最佳实践与常见问题.md`

### 8.3 培训交付物

- 平台使用培训 PPT/视频；
- 联邦学习实操 Lab（含示例数据）；
- 平台熟练度测评问卷。

---

## 9. 下一步建议

1. **召开需求评审会**：确认 Local DP 的参数范围、分类分级标准、平台熟练度目标用户。
2. **分配环境账号**：确保每位开发人员都能运行 `bash scripts/dev-start.sh` 并看到完整界面。
3. **创建项目看板**：按 Phase 0 → Phase 5 拆解任务，设置每周 Review。
4. **先做一个最小可用 Demo**：建议从 Local DP 组件开始，因为它与现有 `l_diversity` 最接近，能最快验证端到端通路。
