# sfwork 二次开发运行说明（自定义 SecretFlow 隐私计算镜像）

本文档说明如何在开发模式下运行 SecretPad 前端、后端，并通过 Docker 部署 Kuscia 集群，且**使用本地二次开发编译的 SecretFlow 隐私计算镜像**执行隐私计算任务。

> 适用范围：sfwork 工作区（`/home/charles/code/sfwork`），包含 `kuscia/`、`secretflow/`、`secretpad/` 三个本地仓库。

---

## 目录

- [1. 环境准备](#1-环境准备)
- [2. 一键启动本地开发环境](#2-一键启动本地开发环境)
- [3. 手动部署与镜像构建](#3-手动部署与镜像构建)
  - [3.4 macOS ARM64 上本地构建 Kuscia 镜像](#34-macos-arm64-apple-silicon-上本地构建-kuscia-镜像)
- [4. 后端服务启动](#4-后端服务启动)
- [5. 前端服务启动](#5-前端服务启动)
- [6. 登录与功能操作](#6-登录与功能操作)
- [7. Nginx / Tomcat / Kuscia 端口配置逻辑](#7-nginx--tomcat--kuscia-端口配置逻辑)
- [8. 常见问题排查](#8-常见问题排查)
  - [8.7 Maven 编译失败：找不到符号（Lombok）](#87-maven-编译失败找不到符号lombok)
  - [8.8 前端依赖安装失败：`node-gyp` / 原生模块编译报错](#88-前端依赖安装失败node-gyp--原生模块编译报错)
  - [8.9 `dev-start.sh` 运行时常见错误速查](#89-dev-startsh-运行时常见错误速查)
    - [8.9.17 端口实际在监听，但脚本反复报"未就绪"](#8917-端口实际在监听但脚本反复报未就绪)
- [9. 附录：端口与文件速查](#9-附录端口与文件速查)
- [10. 配置文件与资源详解](#10-配置文件与资源详解)

---

## 1. 环境准备

### 1.1 系统要求

| 组件 | 版本要求 | 说明 |
|------|---------|------|
| JDK | 17（不支持 22+，例如 JDK 26 会因 Lombok 兼容性问题导致编译失败） | SecretPad 后端编译与运行 |
| Maven | 3.8.8+ | Java 构建工具 |
| Node.js | > 18（18.x 及以上均可；Umi 4 / React 18 与新版 Node 没有明显兼容性问题） | 前端运行环境 |
| pnpm | 8.8.0 | 前端包管理器 |
| Docker | >= 20.10 | 运行 Kuscia 容器 |
| macOS（Apple Silicon） | Docker Desktop 4.x+ | 需使用支持 linux/arm64 的 Kuscia 镜像，必要时本地构建 |
| conda（Miniconda/Anaconda） | 任意 | 构建 SecretFlow wheel 时使用 |
| Git | 任意 | 代码管理 |

验证命令：

```bash
java -version      # openjdk 17
mvn -version       # Apache Maven 3.x
node -v            # v18 及以上均可，例如 v20、v22
pnpm -v            # 8.8.0
docker --version   # >= 20.10
conda env list | grep sf310
```

> **说明**：Kuscia 代码本次未更新，因此继续使用官方 Kuscia 镜像；SecretFlow 使用本地二次开发镜像 `secretflow/sf-privacy-dev:1.15.0.dev-privacy`。

### 1.2 工作目录结构

```
/home/charles/code/sfwork/
├── kuscia/                         # Kuscia Go 源码（本次未改动）
├── secretflow/                     # SecretFlow Python 源码（含 privacy/l_diversity 组件）
│   └── docker/privacy-dev/         # 二次开发镜像 Dockerfile 与 AppImage 模板
├── secretpad/                      # SecretPad Java 后端
│   ├── scripts/
│   │   ├── install-kuscia-only.sh  # 仅部署 Kuscia 容器
│   │   ├── dev-start.sh            # SecretPad 仓库自带的历史脚本，本方案推荐使用 sfwork/scripts/dev-start.sh
│   │   └── dev-stop.sh             # 停止脚本
│   ├── frontend-src/               # React 前端源码（由 scripts/clone-repos.sh 从 secretpad-frontend 仓库克隆）
│   └── target/secretpad.jar        # Maven 构建产物
├── scripts/
│   ├── clone-repos.sh              # 克隆/更新所有子项目（含 secretpad-frontend）
│   ├── dev-start.sh                # 本方案推荐的一键启动脚本
│   └── dev-stop.sh                 # 对应停止脚本
├── docs/                           # 项目文档
└── logs/                           # 后端/前端运行日志
```

---

## 2. 一键启动本地开发环境

### 2.1 使用一键脚本（推荐）

进入 sfwork 根目录，执行：

```bash
cd /home/charles/code/sfwork
bash scripts/dev-start.sh
```

首次运行会自动完成：

1. 检测 JDK 17 / Maven / Node.js / pnpm / Docker / conda `sf310`
2. 基于 `secretflow/` 源码构建自定义 SecretFlow 镜像 `secretflow/sf-privacy-dev:1.15.0.dev-privacy`
3. 部署 Kuscia master + alice + bob 容器，并注册上述自定义镜像作为 AppImage
4. 生成证书、编译 SecretPad 后端 fat jar
5. 启动后端和前端开发服务器

启动成功后会输出：

```text
🌐 前端开发服务器：http://localhost:8000
🔧 后端健康检查：http://localhost:8080/actuator/health
🔒 后端 HTTPS 地址：https://localhost:8443
🐳 自定义 SecretFlow 镜像：secretflow/sf-privacy-dev:1.15.0.dev-privacy
👤 登录账号：admin / 12345678
```

其他常用命令：

```bash
# 仅检查并自动安装/确认运行时
bash scripts/dev-start.sh --check

# 停止后端和前端（保留 Kuscia）
bash scripts/dev-stop.sh

# 同时停止 Kuscia 容器
bash scripts/dev-stop.sh --kuscia
```

### 2.2 环境变量说明

| 环境变量 | 默认值 | 说明 |
|----------|--------|------|
| `PRIVACY_IMAGE` | `secretflow/sf-privacy-dev:1.15.0.dev-privacy` | 自定义 SecretFlow 镜像 tag |
| `PRIVACY_DOCKERFILE` | `Dockerfile.anolis` | 构建 SecretFlow 镜像时使用的 Dockerfile；默认使用 Anolis OS 基础镜像以适配国内网络 |
| `KUSCIA_IMAGE` | （使用 `install-kuscia-only.sh` 官方默认镜像） | 自定义 Kuscia 镜像 tag，常用于 macOS ARM64 本地构建镜像 |
| `CONDA_ENV` | `sf310` | 构建 wheel 时使用的 conda 环境 |

示例：

```bash
PRIVACY_IMAGE="myregistry/sf-privacy:dev" CONDA_ENV="sf310" \
  bash scripts/dev-start.sh
```

在 macOS ARM64 上使用本地构建的 Kuscia 镜像：

```bash
KUSCIA_IMAGE="secretflow/kuscia:sfwork-local" bash scripts/dev-start.sh
```

---

## 3. 手动部署与镜像构建

如果你希望分步操作，或需要排查一键脚本中的某一步，可按以下流程手动执行。

### 3.1 构建二次开发 SecretFlow 镜像

基于 `secretflow/docker/privacy-dev/Dockerfile.anolis` 构建快速验证镜像（默认使用 Anolis OS 与阿里云镜像源，避免在中国大陆无法访问 Docker Hub 时拉取 `secretflow/ubuntu-base-ci` 失败）：

```bash
cd /home/charles/code/sfwork/secretflow

source "$(conda info --base)/etc/profile.d/conda.sh"
conda activate sf310

# 1. 构建 wheel
rm -rf dist build
python -m build --wheel

# 2. 准备构建上下文
mkdir -p docker/privacy-dev
cp dist/secretflow-*.whl docker/privacy-dev/

# 3. 构建镜像
cd docker/privacy-dev

# 默认使用阿里云 PyPI 镜像源与 Anolis OS 基础镜像
docker build . -f Dockerfile.anolis -t secretflow/sf-privacy-dev:1.15.0.dev-privacy

# 若能直接访问 Docker Hub,也可使用原版 Ubuntu 基础镜像：
# docker build . -f Dockerfile \
#   -t secretflow/sf-privacy-dev:1.15.0.dev-privacy

# 若阿里云源出现下载中断/哈希校验失败，可切换到官方 PyPI：
# docker build . -f Dockerfile.anolis \
#   --build-arg PIP_INDEX_URL=https://pypi.org/simple/ \
#   -t secretflow/sf-privacy-dev:1.15.0.dev-privacy
```

产物镜像：

```text
secretflow/sf-privacy-dev:1.15.0.dev-privacy
```

验证镜像内组件已注册：

```bash
docker run --rm secretflow/sf-privacy-dev:1.15.0.dev-privacy \
  'python -c "from secretflow.component.core import Registry; \
              print(Registry.get_definition_by_id(\"privacy/l_diversity:1.0.0\").component_def.name)"'
```

> 注意：`ubuntu-base-ci` 镜像的 `ENTRYPOINT` 是 `/bin/bash -lc`，因此 `docker run` 时整个命令需要用**单引号**包成一组传入。

如需使用官方完整镜像构建方式（方式 B），请参考 `docs/privacy-component-deployment.md` 第 4.1 节。

### 3.2 部署 Kuscia（不部署容器版 SecretPad）

```bash
cd /home/charles/code/sfwork/secretpad

# 关键：通过环境变量指定自定义 SecretFlow 镜像
export SECRETFLOW_IMAGE="secretflow/sf-privacy-dev:1.15.0.dev-privacy"

# 部署 Kuscia master + alice + bob
bash scripts/install-kuscia-only.sh master -P notls
```

`-P notls` 表示不使用 mTLS，本地开发更简便。

部署成功后会创建以下容器（假设当前用户为 `charles`）：

| 容器名 | 说明 |
|--------|------|
| `charles-kuscia-master` | Kuscia Master 控制平面 |
| `charles-kuscia-lite-alice` | Lite 节点 alice |
| `charles-kuscia-lite-bob` | Lite 节点 bob |

默认端口映射（`master` 模式固定）：

| 服务 | 宿主机端口 | 容器端口 | 说明 |
|------|-----------|---------|------|
| Kuscia master gateway | 18080 | 1080 | 节点间认证鉴权 |
| Kuscia master KusciaAPI HTTP | 18082 | 8082 | HTTP API |
| Kuscia master KusciaAPI gRPC | 18083 | 8083 | SecretPad 后端连接此端口 |
| Kuscia master Envoy 内部端口 | 13081 | 80 | `KUSCIA_GW_ADDRESS` 指向此端口 |
| Kuscia lite alice gateway | 28080 | 1080 | alice 对外网关 |
| Kuscia lite alice KusciaAPI gRPC | 28083 | 8083 | alice API gRPC |
| Kuscia lite bob gateway | 38080 | 1080 | bob 对外网关 |
| Kuscia lite bob KusciaAPI gRPC | 38083 | 8083 | bob API gRPC |

验证自定义 AppImage 已注册：

```bash
docker exec -it ${USER}-kuscia-master kubectl get appimage
# 应看到 secretflow-image 使用的镜像为 secretflow/sf-privacy-dev:1.15.0.dev-privacy
```

查看 SecretFlow 镜像是否已导入 Kuscia：

```bash
docker exec -it ${USER}-kuscia-lite-alice kuscia image list
```

### 3.3 源码后端连接 Kuscia 容器

```bash
export KUSCIA_API_ADDRESS=127.0.0.1
export KUSCIA_API_PORT=18083
export KUSCIA_GW_ADDRESS=127.0.0.1:13081
export KUSCIA_PROTOCOL=notls
```

> 也可以修改 `/etc/hosts` 将 `root-kuscia-master`、`root-kuscia-lite-alice`、`root-kuscia-lite-bob` 指向 `127.0.0.1`，但使用环境变量更简单。

### 3.4 macOS ARM64 (Apple Silicon) 上本地构建 Kuscia 镜像

官方 Kuscia 镜像已提供多架构版本（linux/amd64 与 linux/arm64），在 Apple Silicon Mac 上通常可直接运行。若你遇到以下情况，建议本地构建 ARM64 镜像：

- Docker 拉取的 Kuscia 镜像被识别为 `amd64`，容器启动后立即退出或报 `exec format error`。
- 需要修改 Kuscia 源码并验证效果。
- 网络环境无法拉取官方多架构镜像，只能使用本地镜像。

#### 3.4.1 一键构建本地 Kuscia 镜像

sfwork 已提供封装脚本，自动检测当前架构并调用 `kuscia/scripts/build-kuscia-secretflow-images.sh`：

```bash
cd /home/charles/code/sfwork
bash scripts/build-kuscia-image.sh
```

脚本会：

1. 检测宿主机架构（Apple Silicon 上为 `arm64`）。
2. 在 `kuscia/` 目录下编译对应架构的 `kuscia` 二进制。
3. 使用 `docker buildx` 构建 `linux/arm64` 镜像。
4. 为镜像打上本地稳定标签 `secretflow/kuscia:sfwork-local`。

构建完成后，终端会输出类似：

```text
[INFO] Kuscia 镜像构建完成
[INFO] 镜像名（含版本）：secretflow/kuscia:v0.7.0-20250710210000
[INFO] 镜像名（本地标签）：secretflow/kuscia:sfwork-local

[INFO] 使用该镜像启动 sfwork 开发环境：

  KUSCIA_IMAGE=secretflow/kuscia:sfwork-local bash scripts/dev-start.sh
```

#### 3.4.2 使用本地镜像启动开发环境

```bash
cd /home/charles/code/sfwork
KUSCIA_IMAGE=secretflow/kuscia:sfwork-local bash scripts/dev-start.sh
```

或仅部署 Kuscia：

```bash
cd /home/charles/code/sfwork/secretpad
export KUSCIA_IMAGE=secretflow/kuscia:sfwork-local
export SECRETFLOW_IMAGE=secretflow/sf-privacy-dev:1.15.0.dev-privacy
bash scripts/install-kuscia-only.sh master -P notls
```

#### 3.4.3 验证镜像架构

```bash
docker inspect --format='{{.Os}}/{{.Architecture}}' secretflow/kuscia:sfwork-local
# 期望输出：linux/arm64
```

#### 3.4.4 清理与重建

当 Kuscia 源码发生变更后，需要重新构建镜像并清理旧容器：

```bash
bash scripts/build-kuscia-image.sh
bash scripts/dev-start.sh --reset-kuscia
```

> 注意：Kuscia 镜像构建需要 Go 1.24.7+ 和 Docker buildx。首次构建耗时较长，请确保网络可访问 `secretflow-registry.cn-hangzhou.cr.aliyuncs.com` 拉取基础镜像。

---

## 4. 后端服务启动

### 4.1 生成证书和初始化数据库

```bash
cd /home/charles/code/sfwork/secretpad
bash scripts/test/setup.sh
```

执行成功后会生成：

- `config/certs/` — KusciaAPI 客户端证书（含 alice / bob 子目录）
- `config/server.jks` — HTTPS 服务证书
- `db/` — SQLite / H2 数据库目录

### 4.2 构建可执行 JAR

```bash
cd /home/charles/code/sfwork/secretpad
mvn clean install -Dmaven.test.skip=true
```

构建产物：

- **可执行 fat jar**：`target/secretpad.jar`

### 4.3 启动后端

```bash
cd /home/charles/code/sfwork/secretpad

export KUSCIA_API_ADDRESS=127.0.0.1
export KUSCIA_API_PORT=18083
export KUSCIA_GW_ADDRESS=127.0.0.1:13081
export KUSCIA_PROTOCOL=notls

java -Dspring.profiles.active=dev \
     -Dsun.net.http.allowRestrictedHeaders=true \
     -Dserver.port=8443 \
     -jar target/secretpad.jar
```

### 4.4 验证后端启动成功

```bash
curl http://127.0.0.1:8080/actuator/health
# 正常返回：{"status":"UP"}
```

---

## 5. 前端服务启动

### 5.1 安装依赖

```bash
cd /home/charles/code/sfwork/secretpad/frontend-src
pnpm bootstrap
```

### 5.2 配置 API 代理

```bash
cd /home/charles/code/sfwork/secretpad/frontend-src/apps/platform
echo "PROXY_URL=http://127.0.0.1:8080" > .env
```

### 5.3 启动前端开发服务器

```bash
cd /home/charles/code/sfwork/secretpad/frontend-src
pnpm --filter secretpad dev
```

浏览器打开：`http://localhost:8000`

---

## 6. 登录与功能操作

### 6.1 登录

- 地址：`http://localhost:8000`
- 用户名：`admin`
- 密码：`12345678`

### 6.2 验证隐私计算任务

1. 登录后进入「节点管理」，确认 alice / bob 节点在线。
2. 进入「数据管理」，应能看到 `alice-table` 和 `bob-table`。
3. 创建项目，参与方选择 alice 和 bob。
4. 新建任务流，从组件面板找到「隐私计算」分组下的 `l_diversity`。
5. 配置参数、连线、保存并运行。
6. 进入「任务管理」查看状态，应能看到任务从 `Pending` → `Running` → `Succeeded`。

### 6.3 查看任务容器日志

任务运行时，Kuscia 会基于自定义镜像 `secretflow/sf-privacy-dev:1.15.0.dev-privacy` 拉起任务 Pod：

```bash
# 查看 alice 节点上的任务 Pod
docker exec -it ${USER}-kuscia-lite-alice kubectl get pods

# 查看任务日志
docker exec -it ${USER}-kuscia-lite-alice kubectl logs <pod-name>
```

---

## 7. Nginx / Tomcat / Kuscia 端口配置逻辑

### 7.1 Tomcat 端口

| 配置项 | 默认端口 | 作用 |
|--------|---------|------|
| `server.port` | 443 / 8443 | HTTPS 主端口 |
| `server.http-port` | 8080 | HTTP API 端口，前端 dev 代理指向这里 |
| `server.http-port-inner` | 9001 | 内部 RPC 端口 |

### 7.2 Kuscia 端口映射

| 服务 | 容器端口 | Master 宿主机端口 | Alice 宿主机端口 | Bob 宿主机端口 | 说明 |
|------|---------|------------------|-----------------|---------------|------|
| Gateway | 1080 | 18080 | 28080 | 38080 | 节点间认证、任务路由 |
| KusciaAPI HTTP | 8082 | 18082 | 28082 | 38082 | HTTP 管理 API |
| KusciaAPI gRPC | 8083 | 18083 | 28083 | 38083 | SecretPad 后端连接此端口 |
| Envoy Internal | 80 | 13081 | 23081 | 33081 | 数据传输、任务调度内部通道 |

### 7.3 三者如何对接

1. **前端 → 后端**：`PROXY_URL=http://127.0.0.1:8080`
2. **后端 → Kuscia API**：`KUSCIA_API_ADDRESS=127.0.0.1`，`KUSCIA_API_PORT=18083`
3. **后端 → Kuscia Gateway**：`KUSCIA_GW_ADDRESS=127.0.0.1:13081`

---

## 8. 常见问题排查

### 8.1 `install-kuscia-only.sh` 仍拉取官方 SecretFlow 镜像

请确认调用前已正确导出环境变量：

```bash
export SECRETFLOW_IMAGE="secretflow/sf-privacy-dev:1.15.0.dev-privacy"
bash scripts/install-kuscia-only.sh master -P notls
```

`install-kuscia-only.sh` 已修改为支持通过环境变量覆盖镜像，且对于本地已存在的镜像会跳过拉取。

### 8.2 任务执行时提示找不到镜像

1. 确认自定义镜像已导入 Docker：
   ```bash
   docker images | grep sf-privacy-dev
   ```
2. 确认镜像已导入 Kuscia：
   ```bash
   docker exec -it ${USER}-kuscia-lite-alice kuscia image list
   ```
3. 确认 AppImage 已注册：
   ```bash
   docker exec -it ${USER}-kuscia-master kubectl get appimage -o yaml
   ```

### 8.3 后端连接 Kuscia 失败

```bash
export KUSCIA_API_ADDRESS=127.0.0.1
export KUSCIA_API_PORT=18083
export KUSCIA_GW_ADDRESS=127.0.0.1:13081
export KUSCIA_PROTOCOL=notls
```

检查端口：

```bash
ss -tlnp | grep -E ':(18083|13081)\b'
docker ps | grep kuscia
```

### 8.4 后端端口冲突

参考 [无 Docker 运行说明](../无docker运行说明.md) 中的端口冲突排查。

### 8.5 构建 wheel 时依赖缺失

```bash
conda activate sf310
pip install --upgrade build setuptools wheel
```

### 8.6 macOS ARM64 (Apple Silicon) 上 Kuscia 容器无法启动

#### 现象

- `docker ps` 看不到 Kuscia 容器，或容器立即退出。
- `docker logs ${USER}-kuscia-master` 出现 `exec format error` 或 `standard_init_linux.go:228: exec user process caused: exec format error`。
- `docker inspect <kuscia-image>` 显示 `"Architecture": "amd64"`。

#### 排查步骤

1. 确认当前拉取的 Kuscia 镜像架构：
   ```bash
   docker inspect --format='{{.Os}}/{{.Architecture}}' \
     secretflow-registry.cn-hangzhou.cr.aliyuncs.com/secretflow/kuscia:0.13.0b0
   ```
   若输出为 `linux/amd64`，说明 Docker 选择了错误的架构。

2. 检查 Docker Desktop 设置：
   - 打开 **Settings -> General**，确认已勾选 **Virtualization framework**（Apple Silicon 必需）。
   - 打开 **Settings -> Features in development**，建议开启 **Use Rosetta for x86/amd64 emulation on Apple Silicon**（可选，用于兼容部分无 arm64 的镜像）。

3. 若官方镜像仍无法运行，按 [3.4 节](#34-macos-arm64-apple-silicon-上本地构建-kuscia-镜像) 本地构建 ARM64 镜像：
   ```bash
   bash scripts/build-kuscia-image.sh
   KUSCIA_IMAGE=secretflow/kuscia:sfwork-local bash scripts/dev-start.sh
   ```

#### 推荐做法

在 Apple Silicon Mac 上长期开发时，建议始终通过 `scripts/build-kuscia-image.sh` 构建本地 ARM64 镜像，避免 QEMU/Rosetta 模拟带来的性能与稳定性问题。

### 8.7 Maven 编译失败：找不到符号（Lombok 生成的方法/变量 `log`）

#### 现象

编译 `secretpad-common` 时出现大量类似错误：

```text
[ERROR] 找不到符号: 方法 setData(T)
[ERROR] 找不到符号: 变量 log
[ERROR] 无法将类 SecretPadResponseStatus 中的构造器应用到给定类型
```

#### 原因

你当前使用的 JDK 版本过高（如 JDK 26）。SecretPad 要求 JDK 17，Lombok 等注解处理器在 JDK 22+ 上可能无法正常工作，导致生成的 getter/setter/`log` 等符号缺失。

#### 解决方法

`scripts/dev-start.sh` 已支持自动探测常见路径下的 JDK 17（如 `/opt/homebrew/opt/openjdk@17`、`/Library/Java/JavaVirtualMachines/temurin-17.jdk/Contents/Home` 等），若当前 Java 版本不符合要求，脚本会自动切换，无需每次手动导出 `JAVA_HOME`。

如需手动指定，可安装 JDK 17 并指定给 Maven 使用：

```bash
# macOS (Homebrew)
brew install openjdk@17

# 临时指定 JAVA_HOME（当前终端有效）
export JAVA_HOME=/opt/homebrew/opt/openjdk@17
export PATH="$JAVA_HOME/bin:$PATH"

# 验证版本
java -version

# 重新编译
mvn clean install -Dmaven.test.skip=true
```

建议将 JDK 17 加入 shell 配置文件（如 `~/.zshrc`），避免每次手动设置：

```bash
echo 'export JAVA_HOME=/opt/homebrew/opt/openjdk@17' >> ~/.zshrc
echo 'export PATH="$JAVA_HOME/bin:$PATH"' >> ~/.zshrc
source ~/.zshrc
```

### 8.8 前端依赖安装失败：`node-gyp` / 原生模块编译报错

#### 现象

执行 `bash scripts/dev-start.sh` 到前端依赖安装阶段时报错：

```text
.../node_modules/nice-napi install: ModuleNotFoundError: No module named 'distutils'
```

或

```text
gyp ERR! stack Error: `make` failed with exit code: 2
```

#### 原因

`scripts/dev-start.sh` 已放宽 Node.js 版本要求为 **> 18**，因此 Node 18、20、22 等均可使用。但部分前端原生依赖（如 `nice-napi`）在高版本 Node 上可能需要重新编译，若本地缺少 `python`、`make`、`g++` 等构建工具，或 Python 版本与 `node-gyp` 不兼容，就会报错。

#### 解决方法

1. 优先确保本地有 Node 原生模块编译环境（以 Ubuntu/Debian 为例）：
   ```bash
   sudo apt update
   sudo apt install -y build-essential python3 python3-distutils
   ```
   macOS 用户通常只需安装 Xcode Command Line Tools：
   ```bash
   xcode-select --install
   ```

2. 若问题仍无法解决，可切换到 Node 18 或 20 后清理依赖重新安装：
   ```bash
   fnm install 20
   fnm default 20
   fnm use 20

   rm -rf secretpad/frontend-src/node_modules \
          secretpad/frontend-src/apps/*/node_modules \
          secretpad/frontend-src/packages/*/node_modules \
          secretpad/frontend-src/tooling/*/node_modules
   bash scripts/dev-start.sh
   ```

### 8.9 `dev-start.sh` 运行时常见错误速查

本节汇总一键启动脚本 `scripts/dev-start.sh` 各阶段最常见的错误、根因与解决方案。建议首次运行或排查问题时按脚本输出顺序定位。

#### 8.9.1 子项目目录不存在

**现象**

```text
[ERROR] 以下子项目目录不存在:
  - /home/charles/code/sfwork/secretpad
  - /home/charles/code/sfwork/secretflow
  - /home/charles/code/sfwork/secretpad/frontend-src
请先执行子项目克隆脚本:bash scripts/clone-repos.sh
```

**原因**

sfwork 根仓库仅包含编排脚本与文档，Kuscia、SecretFlow、SecretPad 及前端源码需要通过 `scripts/clone-repos.sh` 单独克隆。

**解决方法**

```bash
cd /home/charles/code/sfwork
bash scripts/clone-repos.sh
```

---

#### 8.9.2 Java 版本不满足（JDK 17 未找到或 JDK 22+）

**现象**

```text
[ERROR] 当前 Java 版本为 26, SecretPad 需要 JDK 17 至 JDK 21
```

或脚本提示自动探测 JDK 17 失败。

**原因**

SecretPad 后端基于 Spring Boot 3 + Lombok，JDK 22+ 上注解处理器可能无法正常工作，导致编译失败。`dev-start.sh` 会尝试在常见路径自动探测 JDK 17，若找不到则报错。

**解决方法**

- 安装 JDK 17 并设置 `JAVA_HOME`：
  ```bash
  # macOS (Homebrew)
  brew install openjdk@17
  export JAVA_HOME=/opt/homebrew/opt/openjdk@17
  export PATH="$JAVA_HOME/bin:$PATH"

  # Ubuntu/Debian
  sudo apt install openjdk-17-jdk
  export JAVA_HOME=/usr/lib/jvm/java-17-openjdk
  export PATH="$JAVA_HOME/bin:$PATH"
  ```
- 验证后重试：
  ```bash
  java -version
  bash scripts/dev-start.sh
  ```

---

#### 8.9.3 Maven 版本不足

**现象**

```text
[ERROR] 需要 Maven 3.8.8+,请安装后重试
```

**解决方法**

升级 Maven 到 3.8.8 或更高版本：

```bash
# macOS
brew install maven

# 或手动下载官方二进制包并配置 PATH
# https://maven.apache.org/download.cgi
```

---

#### 8.9.4 Node.js 版本不满足

**现象**

```text
[ERROR] 当前 Node.js 版本为 16.14.0,SecretPad 前端需要 Node.js > 18
```

或

```text
[ERROR] 需要 Node.js > 18,请安装后重试
```

**解决方法**

使用 fnm / nvm 安装并切换 Node 18 及以上：

```bash
fnm install 20
fnm default 20
fnm use 20
node -v  # 应输出 v20.x
```

---

#### 8.9.5 corepack 未找到或 pnpm 版本不对

**现象**

```text
[ERROR] 未找到 corepack,请升级 Node.js 到 16.10+ 或手动安装 pnpm 8.8.0
```

**原因**

`dev-start.sh` 依赖 Node.js 内置的 `corepack` 来管理 pnpm。若 Node 版本过低或未启用 corepack，则会失败。

**解决方法**

确保 Node.js ≥ 18，然后启用 corepack：

```bash
corepack enable
corepack prepare pnpm@8.8.0 --activate
```

---

#### 8.9.6 Docker 未安装或版本不足

**现象**

```text
[ERROR] 需要 Docker 20.10+,当前版本 19.03
```

或

```text
[ERROR] 未找到 Docker,请手动安装 Docker >= 20.10
```

**解决方法**

安装/升级 Docker Desktop 或 Docker Engine 到 20.10+，并确保当前用户有权执行 `docker` 命令（通常需将用户加入 `docker` 组后重新登录）。

---

#### 8.9.7 conda 环境 `sf310` 不存在

**现象**

```text
[ERROR] Conda 环境 sf310 不存在,请先创建:conda create -n sf310 python=3.10 -y
```

**解决方法**

```bash
conda create -n sf310 python=3.10 -y
conda activate sf310
pip install --upgrade build setuptools wheel
```

---

#### 8.9.8 端口冲突

**现象**

```text
[ERROR] 端口 8080 被其他进程(pid 12345)占用,无法启动后端
[ERROR] 端口 8000 被其他进程(pid 12346)占用,无法启动前端
[ERROR] 端口 18083 已被占用(pid 12347),无法部署 Kuscia
```

**原因**

本脚本固定使用以下端口：

| 服务 | 端口 |
|------|------|
| 后端 HTTP | 8080 |
| 后端 HTTPS | 8443 |
| 前端 dev server | 8000 |
| Kuscia master gateway | 18080 |
| Kuscia master KusciaAPI HTTP | 18082 |
| Kuscia master KusciaAPI gRPC | 18083 |
| Kuscia master Envoy 内部端口 | 13081 |

**解决方法**

- 若端口被本脚本上一次残留进程占用，先执行停止脚本：
  ```bash
  bash scripts/dev-stop.sh
  # 如需同时清理 Kuscia：
  bash scripts/dev-stop.sh --kuscia
  ```
- 若被其他服务占用，找到并停止对应进程：
  ```bash
  lsof -iTCP:8080 -sTCP:LISTEN
  # 或 Linux
  ss -tlnp | grep :8080
  kill <pid>
  ```

---

#### 8.9.9 证书生成失败

**现象**

脚本在"生成 KusciaAPI 证书与后端 JKS"步骤报错退出，或后续后端启动报 `Keystore` / `SSL` 相关异常。

**原因**

`secretpad/scripts/test/setup.sh` 依赖 `openssl`、`keytool` 等工具生成证书。若工具缺失、目录权限不足，或已有损坏的 `config/server.jks`，会导致失败。

**解决方法**

```bash
cd /home/charles/code/sfwork/secretpad
rm -f config/server.jks
rm -rf config/certs/
bash scripts/test/setup.sh
```

---

#### 8.9.10 Maven 编译失败

**现象**

```text
[ERROR] 后端编译失败:未找到 target/secretpad.jar
```

或编译日志中出现找不到符号、依赖下载失败、Lombok 错误等。

**常见原因与解决**

| 错误 | 原因 | 解决 |
|------|------|------|
| `找不到符号: 变量 log`、`找不到符号: 方法 setXxx` | JDK 版本过高（22+）导致 Lombok 失效 | 切换 JDK 17（见 8.9.2） |
| 依赖下载失败、连接超时 | Maven 仓库网络问题 | 配置阿里云镜像或科学上网，重试 `mvn clean install -Dmaven.test.skip=true` |
| `mvn` 命令直接失败 | Maven 版本不足或环境变量问题 | 见 8.9.3 |

---

#### 8.9.11 SecretFlow wheel 构建失败

**现象**

```text
[ERROR] 当前 conda 环境 sf310 缺少 python build 模块
```

或

```text
[ERROR] 未找到构建出的 wheel 文件
```

**解决方法**

```bash
conda activate sf310
pip install --upgrade build setuptools wheel
```

然后清理构建产物重试：

```bash
cd /home/charles/code/sfwork/secretflow
rm -rf dist build
python -m build --wheel
```

---

#### 8.9.12 Docker 镜像构建失败

**现象**

```text
[ERROR] docker build ... 返回非零退出码
```

或基础镜像拉取失败、PyPI 下载超时等。

**常见原因与解决**

| 错误 | 原因 | 解决 |
|------|------|------|
| 拉取 `secretflow/ubuntu-base-ci` 失败 | 国内无法访问 Docker Hub | 使用默认的 `Dockerfile.anolis`（基于 Anolis OS 与阿里云源） |
| PyPI 下载超时/哈希校验失败 | 默认阿里云 PyPI 不稳定 | 切换官方源：`PRIVACY_DOCKERFILE=Dockerfile.anolis bash scripts/dev-start.sh` 仍失败时，可手动在 Dockerfile 中调整 `PIP_INDEX_URL` |
| 构建缓存导致奇怪错误 | Docker build cache 污染 | `docker build --no-cache ...` |

若镜像已存在且希望跳过构建，可直接删除后重试，或先删除旧镜像：

```bash
docker rmi secretflow/sf-privacy-dev:1.15.0.dev-privacy
bash scripts/dev-start.sh
```

---

#### 8.9.13 Kuscia 部署失败或端口未就绪

**现象**

```text
[ERROR] Kuscia API gRPC 在 127.0.0.1:18083 上未就绪,请查看日志
```

或

```text
Whether to retain k3s data?(y/n):
```

输入后脚本卡住或退出。

**原因**

Kuscia 启动较慢，master 容器可能需要数十秒才能就绪；若之前残留了损坏的 Kuscia 数据目录，也可能导致启动失败。

**解决方法**

1. 查看 Kuscia 容器日志：
   ```bash
   docker logs -f ${USER}-kuscia-master
   ```
2. 若首次部署提示是否保留 k3s 数据，建议输入 `n`（全新部署）。
3. 若反复失败，重置 Kuscia 后重试：
   ```bash
   bash scripts/dev-start.sh --reset-kuscia
   ```

---

#### 8.9.14 自定义镜像未导入 Kuscia Lite / 任务提示找不到镜像

**现象**

Kuscia 任务长时间处于 `Pending`，或任务日志中出现 `ErrImagePull`、`ImagePullBackOff`。

**原因**

Kuscia 使用自己的镜像存储，宿主机 Docker 中的本地镜像不会自动对 Kuscia 可见。`dev-start.sh` 会在首次启动时导入镜像，但如果 Kuscia 容器是之前启动的，可能不会重新导入。

**解决方法**

1. 确认自定义镜像在宿主机 Docker 中存在：
   ```bash
   docker images | grep sf-privacy-dev
   ```
2. 确认已导入 Kuscia Lite：
   ```bash
   docker exec -it ${USER}-kuscia-lite-alice kuscia image list
   docker exec -it ${USER}-kuscia-lite-bob kuscia image list
   ```
3. 若不存在，手动导入：
   ```bash
   docker save secretflow/sf-privacy-dev:1.15.0.dev-privacy | \
     docker exec -i ${USER}-kuscia-lite-alice kuscia image load
   docker save secretflow/sf-privacy-dev:1.15.0.dev-privacy | \
     docker exec -i ${USER}-kuscia-lite-bob kuscia image load
   ```
4. 确认 AppImage 已注册：
   ```bash
   docker exec -it ${USER}-kuscia-master kubectl get appimage
   ```

---

#### 8.9.15 后端启动失败或健康检查不通

**现象**

```text
[ERROR] 后端 HTTP 在 127.0.0.1:8080 上未就绪,请查看日志
```

**排查步骤**

1. 查看后端日志：
   ```bash
   tail -f /home/charles/code/sfwork/logs/backend.log
   ```
2. 常见原因：
   - `target/secretpad.jar` 不存在或损坏 → 重新执行 Maven 编译。
   - Kuscia gRPC 18083 未就绪 → 先确保 Kuscia 正常运行。
   - 端口 8080/8443 冲突 → 见 8.9.8。
   - 证书/JKS 生成失败 → 见 8.9.9。

---

#### 8.9.16 前端启动失败或页面无法访问

**现象**

```text
[ERROR] 前端开发服务器 在 127.0.0.1:8000 上未就绪,请查看日志
```

**排查步骤**

1. 查看前端日志：
   ```bash
   tail -f /home/charles/code/sfwork/logs/frontend.log
   ```
2. 常见原因：
   - `node_modules` 损坏或高版本 Node 原生模块编译失败 → 见 8.8。
   - 端口 8000 冲突 → 见 8.9.8。
   - 后端 HTTP 8080 未启动或 `.env` 中 `PROXY_URL` 配置错误 → 检查 `secretpad/frontend-src/apps/platform/.env`。

---

#### 8.9.17 端口实际在监听，但脚本反复报"未就绪"

**现象**

执行 `bash scripts/dev-start.sh` 时，在"等待 Kuscia API gRPC 就绪:127.0.0.1:18083"步骤报错：

```text
[ERROR] Kuscia API gRPC 在 127.0.0.1:18083 上未就绪,请查看日志
```

但手动检查却发现端口确实在监听：

```bash
ss -tln | grep 18083
# 输出:
# LISTEN 0 4096 0.0.0.0:18083 0.0.0.0:*
# LISTEN 0 4096 [::]:18083 [::]:*
```

**原因**

`scripts/dev-start.sh` 内部使用 `grep -E ":$port\\b"` 精确匹配端口号。在某些 Linux 发行版/GNU grep 版本（如 grep 3.12）下，`\\b` 在纯数字端口后不能正确匹配单词边界，导致 `port_is_listening` 误判端口未就绪。此外，原脚本优先使用 `lsof`，而 Linux 上普通用户运行 `lsof` 查看 Docker 映射端口时也可能因权限问题失败。

**解决方法**

1. 先确认服务本身确实已正常启动（以 Kuscia 为例）：
   ```bash
   # 容器内查看 8083 是否在监听
   docker exec ${USER}-kuscia-master ss -tln | grep 8083
   # 查看 Kuscia 日志确认 kusciaapi 模块已 ready
   docker logs --tail 50 ${USER}-kuscia-master | grep -E 'kusciaapi|8083|grpc server listening'
   ```
2. 如果确认服务本身正常，则是脚本端口检测逻辑问题，请拉取/更新最新 `scripts/dev-start.sh` 后重试。修复后的脚本已根据操作系统自动选择检测方式：
   - **Linux**：优先使用 `ss`，降级到 `netstat`；正则使用 `:port($|[[:space:]])` 替代 `:port\\b`
   - **macOS**：使用 `lsof`（macOS 没有 `ss`）
3. 作为临时绕过，可跳过该步骤继续后续启动：直接手动启动后端和前端即可（此时 Kuscia、后端 jar、前端依赖均已就绪）：
   ```bash
   cd /home/charles/code/sfwork/secretpad
   export KUSCIA_API_ADDRESS=127.0.0.1
   export KUSCIA_API_PORT=18083
   export KUSCIA_GW_ADDRESS=127.0.0.1:13081
   export KUSCIA_PROTOCOL=notls
   nohup java -Dspring.profiles.active=dev \
        -Dsun.net.http.allowRestrictedHeaders=true \
        -Dserver.port=8443 \
        -jar target/secretpad.jar > ../logs/backend.log 2>&1 &
   echo $! > ../logs/backend.pid

   cd /home/charles/code/sfwork/secretpad/frontend-src
   nohup corepack pnpm --filter secretpad dev > ../logs/frontend.log 2>&1 &
   echo $! > ../logs/frontend.pid
   ```

---

#### 8.9.18 端口 8000 被旧的 umi dev 进程占用

**现象**

执行 `bash scripts/dev-start.sh` 时端口检查报错：

```text
[ERROR] 端口 8000 被其他进程(pid 33716)占用,无法启动前端
```

但 `bash scripts/dev-stop.sh` 提示找不到前端 PID 文件。

**原因**

之前通过 Umi dev server 启动的前端进程（`umi/bin/forkedDev.js dev`）没有被正常停止，PID 文件丢失后 `dev-stop.sh` 无法识别并清理。

**解决方法**

1. 查看占用进程：
   ```bash
   ss -tlnp | grep 8000
   # 或
   lsof -iTCP:8000 -sTCP:LISTEN
   ```
2. 结束该进程：
   ```bash
   kill <pid>
   ```
3. 重新执行 `bash scripts/dev-start.sh`。

---

#### 8.9.19 构建镜像时提示 `Dockerfile.anolis: no such file or directory`

**现象**

```text
ERROR: failed to build: failed to solve: failed to read dockerfile: open Dockerfile.anolis: no such file or directory
```

**原因**

`secretflow/docker/privacy-dev/` 目录下只有 `Dockerfile`，没有 `Dockerfile.anolis`。

**解决方法**

在 `.env` 中显式指定：

```bash
PRIVACY_DOCKERFILE=Dockerfile
```

或在执行时覆盖：

```bash
PRIVACY_DOCKERFILE=Dockerfile bash scripts/dev-start.sh --reset-kuscia
```

---

#### 8.9.20 Docker 镜像中运行的 SecretFlow 仍是旧版本

**现象**

任务日志中出现旧版本号，例如：

```text
Secretflow 1.15.0.dev20260709
Build time (Jul 09 2026, 09:38:20)
```

但本地构建的 wheel 是 `secretflow-1.15.0.dev20260711`。

**原因 1：构建上下文残留旧 wheel**

`COPY secretflow-*.whl /tmp/` 会复制多个 wheel，而 `find ... | head -1` 可能取到旧版本。

**解决方法**

`dev-start.sh` 在复制 wheel 前会清理 `docker/privacy-dev/secretflow-*.whl`，确保只保留最新构建的版本。如手动构建，请先执行：

```bash
rm -f secretflow/docker/privacy-dev/secretflow-*.whl
```

**原因 2：Kuscia 内部镜像存储残留旧镜像**

`--reset-kuscia` 删除容器后，`install-kuscia-only.sh` 发现 Kuscia 内部已有同名同 tag 的镜像，会跳过导入，导致实际使用的是旧 digest。

**解决方法**

`dev-start.sh` 在 `--reset-kuscia` 模式下会强制重新导入本地 Docker 镜像到 Kuscia Lite 节点。也可以手动导入：

```bash
docker save secretflow/sf-privacy-dev:1.15.0.dev-privacy | \
  docker exec -i ${USER}-kuscia-lite-alice kuscia image load
docker save secretflow/sf-privacy-dev:1.15.0.dev-privacy | \
  docker exec -i ${USER}-kuscia-lite-bob kuscia image load
```

---

#### 8.9.21 组件运行时提示 `No module named 'pydantic'`

**现象**

自定义镜像构建失败或任务运行失败：

```text
ModuleNotFoundError: No module named 'pydantic'
```

**原因**

数据分类分级组件从 `privacy-local-agent` 移植，使用 `pydantic` 进行模型校验，但 SecretFlow 默认依赖未声明该包。

**解决方法**

在 `secretflow/docker/privacy-dev/Dockerfile` 中安装 `pydantic`：

```dockerfile
RUN pip install --no-cache-dir pydantic
```

---

#### 8.9.22 数据分类分级任务失败：`input size mismatch, expect [output_ds, report]`

**现象**

任务节点 `privacy/data_classification` 失败，日志中出现：

```text
input size<1> mismatch, expect [name: "output_ds"..., name: "report"...] but got ['xxx_node_2_output_0']
```

**原因**

`data_classification` 组件定义了两个输出：`output_ds`（结果表）和 `report`（报告）。SecretFlow 要求运行时传入的 `output_uris` 数量与组件输出定义完全一致。如果前端/图中只配置了一个输出 anchor，Kuscia 只会生成一个输出 URI，导致校验失败。

**解决方法**

在前端模板或图定义中，为 `data_classification` 节点配置两个输出：

```json
"outputs": ["node-2-output-0", "node-2-output-1"]
```

`node-2-output-0` 对应 `output_ds`，`node-2-output-1` 对应 `report`。即使 report 不连接到下游节点，也必须声明该输出 anchor。

---

#### 8.9.23 前端 `pnpm lint:typing` 存在大量 pre-existing 类型错误

**现象**

新增模板后执行类型检查，出现大量与本次改动无关的错误，例如：

```text
src/util/valtio-helper.ts(23,46): error TS2344: Type 'T' does not satisfy the constraint ...
src/services/secretpad/CenterDataSyncController.ts(23,5): error TS2769: No overload matches ...
```

**原因**

这些错误在修改前已存在，主要是由于自动生成的 API 类型（`src/services/secretpad/typings.d.ts`）与部分业务代码不同步。`pnpm lint:typing` 仅做静态检查，不影响 Umi 构建。

**解决方法**

1. 确认报错文件不在本次改动的 git diff 中。
2. 如果只需要验证构建，`pnpm --filter secretpad build` 可以成功（Umi 4 使用 Babel 转译，不阻塞类型错误）。
3. 要彻底修复，需要同步后端 OpenAPI 生成最新的 `typings.d.ts`，或单独修复各类型不一致点。本模块开发不涉及这些文件的修改。

---

#### 8.9.24 打开 DAG 页面报错 `Unhandled Rejection (Error): invalid port id`

**现象**

访问训练流 DAG 页面时白屏，控制台报错：

```text
Unhandled Rejection (Error): invalid port id
```

**原因**

前端 DAG 引擎使用 `splitPortId(portId)` 解析连接桩 ID，要求端口 ID 必须是 5 段格式：

```text
{dagId}-node-{nodeIndex}-input-{portIndex}
{dagId}-node-{nodeIndex}-output-{portIndex}
```

例如 `idteztkj-node-1-output-0`。如果通过 API 手动创建图时使用了简写的 `node-1-output-0`（只有 4 段），或者 `dagId` 本身包含 `-`，都会导致解析失败。

**解决方法**

1. 删除或重建有问题的图，确保节点 ID 为 `{graphId}-node-{index}`，端口 ID 为 `{graphId}-node-{index}-input-{portIndex}` 或 `{graphId}-node-{index}-output-{portIndex}`。
2. 通过前端模板创建图时，模板代码已按此规则生成 node id 和 port id，无需额外处理。
3. 如需通过 API 测试，参考如下结构：

```json
{
  "graphNodeId": "{graphId}-node-1",
  "inputs": [],
  "outputs": ["{graphId}-node-1-output-0"]
}
```

---

## 9. 附录：端口与文件速查

### 9.1 关键端口

| 服务 | 默认端口 | 说明 |
|------|---------|------|
| 前端 dev 服务器 | 8000 | 开发访问地址 |
| 后端 HTTP | 8080 | API 服务端口 |
| 后端内部 HTTP | 9001 | 节点间通信端口 |
| 后端 HTTPS | 8443 | 主端口 |
| Kuscia master gateway | 18080 | 容器 1080 |
| Kuscia master KusciaAPI HTTP | 18082 | 容器 8082 |
| Kuscia master KusciaAPI gRPC | 18083 | 容器 8083，后端连接此端口 |
| Kuscia master Envoy 内部端口 | 13081 | 容器 80 |
| Kuscia lite alice gRPC | 28083 | 容器 8083 |
| Kuscia lite bob gRPC | 38083 | 容器 8083 |

### 9.2 重要文件

| 文件/目录 | 作用 |
|----------|------|
| `secretpad/target/secretpad.jar` | 后端可执行 fat jar |
| `secretpad/config/application-dev.yaml` | 开发环境配置 |
| `secretpad/config/certs/` | KusciaAPI 证书 |
| `secretpad/frontend-src/apps/platform/.env` | 前端代理配置 |
| `secretflow/docker/privacy-dev/Dockerfile` | 二次开发镜像 Dockerfile |
| `secretflow/docker/privacy-dev/app_image.yaml` | Kuscia AppImage 模板 |
| `scripts/dev-start.sh` | 一键启动完整开发环境 |
| `scripts/dev-stop.sh` | 停止开发环境 |
| `scripts/build-kuscia-image.sh` | 本地构建 Kuscia Docker 镜像（适用于 macOS ARM64 等场景） |
| `logs/backend.log` | 后端日志 |
| `logs/frontend.log` | 前端日志 |

### 9.3 常用命令

```bash
# 一键启动完整开发环境（使用自定义 SecretFlow 镜像）
bash scripts/dev-start.sh

# 停止后端和前端（保留 Kuscia）
bash scripts/dev-stop.sh

# 同时停止 Kuscia 容器
bash scripts/dev-stop.sh --kuscia

# 仅构建自定义 SecretFlow 镜像
cd /home/charles/code/sfwork/secretflow
source "$(conda info --base)/etc/profile.d/conda.sh"
conda activate sf310
rm -rf dist build
python -m build --wheel
cp dist/secretflow-*.whl docker/privacy-dev/
cd docker/privacy-dev
docker build . -f Dockerfile -t secretflow/sf-privacy-dev:1.15.0.dev-privacy

# 仅部署 Kuscia（不启动容器版 SecretPad）
cd /home/charles/code/sfwork/secretpad
export SECRETFLOW_IMAGE="secretflow/sf-privacy-dev:1.15.0.dev-privacy"
bash scripts/install-kuscia-only.sh master -P notls

# 启动后端
export KUSCIA_API_ADDRESS=127.0.0.1
export KUSCIA_API_PORT=18083
export KUSCIA_GW_ADDRESS=127.0.0.1:13081
export KUSCIA_PROTOCOL=notls
java -Dspring.profiles.active=dev \
     -Dsun.net.http.allowRestrictedHeaders=true \
     -Dserver.port=8443 \
     -jar target/secretpad.jar

# 启动前端
cd /home/charles/code/sfwork/secretpad/frontend-src
echo "PROXY_URL=http://127.0.0.1:8080" > apps/platform/.env
pnpm bootstrap
pnpm --filter secretpad dev

# 查看 Kuscia 容器
docker ps | grep kuscia

# 查看 Kuscia 日志
docker logs -f ${USER}-kuscia-master

# 查看 AppImage
docker exec -it ${USER}-kuscia-master kubectl get appimage
```

---

## 10. 配置文件与资源详解

本节说明 SecretPad 运行所需的关键配置文件、资源文件的位置、作用、关键参数含义以及修改方式。二次开发过程中，若需要调整端口、数据源、组件显示、Kuscia 连接方式等，通常只需修改这些文件（或对应环境变量），无需改动代码。

### 10.1 `config/application.yaml`（基础配置）

位置：`secretpad/config/application.yaml`  
作用：Spring Boot 主配置文件，所有运行环境都会加载；特定 profile（如 `dev`、`edge`、`p2p`、`test`）会在此基础上覆盖同名配置。

| 配置分组 | 关键参数 | 默认值 | 含义 |
|---|---|---|---|
| `server` | `http-port` | `8080` | HTTP API 端口，前端 dev 代理默认指向此处 |
| `server` | `http-port-inner` | `9001` | 内部节点通信端口（部分同步接口免认证） |
| `server` | `port` | `443` | HTTPS 主端口；开发时常用 `-Dserver.port=8443` 覆盖 |
| `server.ssl` | `key-store` | `file:./config/server.jks` | HTTPS 证书密钥库 |
| `server.ssl` | `key-store-password` | `${KEY_PASSWORD:secretpad}` | JKS 密码，可通过环境变量 `KEY_PASSWORD` 覆盖 |
| `spring.datasource.default` | `jdbc-url` | `jdbc:sqlite:./db/secretpad.sqlite` | 业务数据库，默认 SQLite |
| `spring.datasource.quartz` | `jdbc-url` | `jdbc:h2:./db/secretpadQuartz.mv.db` | Quartz 定时任务专用 H2 数据源 |
| `spring.flyway.default.locations` | — | `filesystem:./config/schema/center` | Flyway 迁移脚本目录，随部署模式切换 |
| `kusciaapi` | `protocol` | `${KUSCIA_PROTOCOL:tls}` | KusciaAPI 全局协议：`tls` 或 `notls`/`grpc` |
| `kuscia.nodes[0]` | `domainId` / `host` / `port` | `kuscia-system` / `root-kuscia-master` / `8083` | Master 节点连接信息，可被 `KUSCIA_API_ADDRESS`、`KUSCIA_API_PORT` 覆盖 |
| `kuscia.nodes[1/2]` | — | alice / bob | Lite 节点连接信息；开发时通常只与 Master 直接交互 |
| `secretpad.platform-type` | — | `CENTER` | 平台架构：`CENTER`、`EDGE`、`P2P`/`AUTONOMY` |
| `secretpad.deploy-mode` | — | `${DEPLOY_MODE:ALL-IN-ONE}` | 部署模式：`MPC`、`TEE`、`ALL-IN-ONE` |
| `secretpad.node-id` | — | `kuscia-system` | 当前 SecretPad 实例所属节点 ID |
| `secretpad.gateway` | — | `${KUSCIA_GW_ADDRESS:127.0.0.1:80}` | Kuscia Gateway 地址，任务调度和数据传输使用 |
| `secretpad.auth` | `enabled` | `true` | 是否启用登录认证；`test` profile 关闭 |
| `secretpad.data.dir-path` | — | `/app/data/` | 用户上传文件与临时数据目录 |
| `secretpad.datasync` | `center` / `p2p` | `true` / `false` | 多节点数据同步模式开关 |
| `secretpad.version.*` | — | 多个镜像版本 | 用于注册 AppImage 时声明依赖镜像版本 |
| `secretpad.component.hide` | — | 若干组件 ID | 前端不显示的底层组件列表 |
| `secretpad.data-proxy.enabled` | — | `${DATAPROXY_ENABLE:true}` | 是否启用 Data Proxy |
| `secretpad.scql.enabled` | — | `${SCQL_ENABLE:true}` | 是否启用 SCQL 功能 |
| `sfclusterDesc.deviceConfig` | `spu` / `heu` | JSON 字符串 | SPU/HEU 安全计算协议与参数 |
| `sfclusterDesc.rayFedConfig` | `crossSiloCommBackend` | `brpc_link` | 跨节点通信后端 |
| `inner-port.path` | — | 4 条路径 | 可通过 9001 端口免认证访问的内部接口 |
| `ip.block` | `enable` / `list` | `true` / 多个 CIDR | SSRF 防护 IP 黑名单 |

修改方式：
- 推荐：通过环境变量覆盖，如 `KUSCIA_API_ADDRESS=127.0.0.1`。
- 也可直接编辑 YAML，但生产环境建议保留默认文件，仅通过环境变量注入差异配置。

### 10.2 多环境 Profile 配置

位置：`secretpad/config/application-dev.yaml`、`application-edge.yaml`、`application-p2p.yaml`、`application-test.yaml`

| 文件 | 生效方式 | 主要覆盖内容 |
|---|---|---|
| `application-dev.yaml` | `-Dspring.profiles.active=dev` | `platform-type: CENTER`；Kuscia 节点端口改为 `18083/28083/38083`；日志路径改为 `../log`；Gateway 默认 `127.0.0.1:18301`（一键脚本中通过 `KUSCIA_GW_ADDRESS=127.0.0.1:13081` 覆盖） |
| `application-edge.yaml` | `spring.profiles.active=edge` | `platform-type: EDGE`；Flyway 使用 `schema/edge`；只配置一个 Lite 节点；定义边缘节点转发/允许访问的 API 路径 |
| `application-p2p.yaml` | `spring.profiles.active=p2p` | `platform-type: AUTONOMY`；Flyway 使用 `schema/p2p`；开启 P2P 数据同步；配置 `unregister-mapping` 禁用中心化接口 |
| `application-test.yaml` | 单元测试 | 关闭认证、关闭 job 同步、使用 `../tmp/` 临时目录 |

切换 profile 示例：

```bash
java -Dspring.profiles.active=dev -jar target/secretpad.jar
```

### 10.3 组件定义与国际化资源

组件元数据：
- 位置：`secretpad/config/components/*.json`
- 加载配置项：`component.spec.location`，默认 `./config/components`
- 文件：
  - `secretflow.json` — SecretFlow 官方组件（含 `privacy/l_diversity:1.0.0` 等）
  - `secretpad.json` — SecretPad 平台组件（如 `read_data/datatable`）
  - `secretpad_tee.json`、`trustedflow.json`、`scql.json` — TEE、TrustedFlow、SCQL 组件

组件国际化：
- 位置：`secretpad/config/i18n/*.json`
- 加载配置项：`component.i18n.location`，默认 `./config/i18n`
- 文件名与 `components/` 一一对应，提供前端展示的中文/英文名称、参数说明等。

修改方式：新增或调整组件时，需同时修改 `components/` 与 `i18n/` 下对应 JSON，然后重启后端。仅修改 JSON 无需重新编译 Java。

### 10.4 Kuscia 节点序列化配置

位置：`secretpad/config/kuscia/{domainId}`（目录下为以域 ID 命名的序列化文件）

这些文件是后端根据 `kuscia.nodes` YAML 配置生成的 Java 序列化对象（`KusciaGrpcConfig`），用于缓存 Kuscia 连接信息。例如 `config/kuscia/alice/alice` 中包含 host、port、协议、证书路径等。

> **注意**：不建议手工修改这些序列化文件。若 Kuscia 连接信息变更，直接修改 `application*.yaml` 或对应环境变量，后端会在启动或重新加载时更新它们。

### 10.5 证书与 HTTPS 密钥库

位置：`secretpad/config/certs/` 与 `secretpad/config/server.jks`

| 文件/目录 | 作用 |
|---|---|
| `certs/ca.crt`、`ca.key`、`ca.srl` | 本地开发 CA |
| `certs/client.crt`、`client.pem`、`token` | Master 节点 KusciaAPI 客户端证书与 Token |
| `certs/alice/`、`certs/bob/`、`certs/tee/` | 各 Lite 节点的证书与 Token |
| `config/server.jks` | SecretPad HTTPS 服务证书密钥库 |

生成方式：执行 `secretpad/scripts/test/setup.sh`，会根据 `application*.yaml` 中配置重新生成上述证书与 JKS。

### 10.6 数据库迁移脚本

位置：`secretpad/config/schema/`

```text
schema/
├── center/       # CENTER 模式下的业务表迁移脚本
├── edge/         # EDGE 模式下的业务表迁移脚本
├── p2p/          # P2P/AUTONOMY 模式下的业务表迁移脚本
└── quartz/       # Quartz 定时任务表迁移脚本
```

每个目录下按 `V1__init.sql`、`V2__0.8.0.sql` 等 Flyway 版本号命名。Flyway 启动时会根据 `spring.flyway.default.locations` 自动执行未应用的脚本。**不要重命名或删除已有脚本**，否则会导致 Flyway 校验失败。

### 10.7 `ownerId`

位置：`secretpad/config/ownerId`

内容示例：

```text
kuscia-system
```

表示当前 SecretPad 实例对应的 Kuscia 域 ID，后端启动时会读取。在 EDGE/P2P 模式下应与 `NODE_ID` 环境变量保持一致。

### 10.8 前端代理配置

位置：`secretpad/frontend-src/apps/platform/.env`（不存在时手动创建）

开发模式下，Umi dev server 通过 `config/proxy.ts` 读取 `PROXY_URL`，将 `/api` 请求转发到后端：

```bash
echo "PROXY_URL=http://127.0.0.1:8080" > secretpad/frontend-src/apps/platform/.env
```

若后端 HTTP 端口改为其他值（如 `8081`），只需修改 `.env` 中的 `PROXY_URL` 并重启前端 dev server：

```bash
pnpm --filter secretpad dev
```

---

**说明**：本文档基于 sfwork 二次开发实践整理，使用本地构建的 `secretflow/sf-privacy-dev:1.15.0.dev-privacy` 镜像替代官方 SecretFlow 镜像。如遇版本升级导致命令变化，请以仓库最新 README 和 `docs/privacy-component-deployment.md` 为准。
