# ============================================================================
# sfwork 二次开发环境变量配置示例
# ============================================================================
# 用法：
#   1. 复制本文件为 .env：
#        cp .env.example .env
#   2. 修改 .env 中的值
#   3. 运行 scripts/dev-start.sh 或 scripts/dev-stop.sh 时会自动读取
#
# 说明：
#   - .env 不会被提交到 git（已在 .gitignore 中排除）
#   - 所有变量均为可选；不配置时脚本使用内置默认值
#   - 环境变量优先级：当前 shell 环境 > .env 文件 > 脚本内置默认值
#   - 可通过 DEV_START_ENV_FILE 指定其他 env 文件路径
# ============================================================================

# ------------------------------------------------------------------
# SecretFlow 镜像配置
# ------------------------------------------------------------------
# 自定义 SecretFlow 镜像 tag，dev-start.sh 会将其注册到 Kuscia 并用于任务执行
PRIVACY_IMAGE=secretflow/sf-privacy-dev:1.15.0.dev-privacy

# 自定义 SecretFlow 镜像 Dockerfile 文件名
# secretflow/docker/privacy-dev/ 目录下目前只有 Dockerfile，因此默认使用 Dockerfile
PRIVACY_DOCKERFILE=Dockerfile

# ------------------------------------------------------------------
# Kuscia 镜像配置
# ------------------------------------------------------------------
# 自定义 Kuscia 镜像 tag。
# 在 macOS ARM64 (Apple Silicon) 上若官方 x86_64 镜像无法运行，
# 可先执行 bash scripts/build-kuscia-image.sh 构建本地镜像，再取消下面注释：
# KUSCIA_IMAGE=secretflow/kuscia:sfwork-local

# ------------------------------------------------------------------
# Python / conda 配置
# ------------------------------------------------------------------
# 构建 SecretFlow wheel 时使用的 conda 环境名
CONDA_ENV=sf310

# ------------------------------------------------------------------
# Kuscia 安装目录
# ------------------------------------------------------------------
# Kuscia Docker 部署时的数据目录；--reset-kuscia 时会清空该目录
INSTALL_DIR=$HOME/kuscia

# ------------------------------------------------------------------
# 日志与运行时目录
# ------------------------------------------------------------------
# PID 文件、后端/前端聚合日志存放目录；默认在 sfwork 根目录下
LOG_DIR=$SFWORK_ROOT/logs

# ------------------------------------------------------------------
# sudo 密码（仅非 Docker 本地开发时使用）
# ------------------------------------------------------------------
# run-all-no-docker.sh 需要 sudo 来绑定 53/80 等特权端口。
# 若当前用户未配置免密 sudo，请在这里设置密码。
# ⚠️ 本文件已被 .gitignore 忽略，请勿将 .env 提交到 git。
# SUDO_PWD=

# ------------------------------------------------------------------
# 自定义 env 文件路径（通常不需要在 .env 里设置）
# ------------------------------------------------------------------
# 示例：DEV_START_ENV_FILE=/path/to/my-dev.env
# DEV_START_ENV_FILE=
